source:
  type: docker
  repo: ghcr.io/mealie-recipes/mealie
  current_version: v3.10.2
  version_template: regex:^v(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-mealie
build:
  image: ghcr.io/mealie-recipes/mealie
  version_template: v{{major}}.{{minor}}.{{patch}}
changelog:
  source: github-releases
  repo: mealie-recipes/mealie
  version_template: v{{major}}.{{minor}}.{{patch}}
test:
  enable: true
