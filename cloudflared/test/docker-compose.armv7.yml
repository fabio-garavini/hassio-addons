services:
  cloudflared-armv7:
    image: fabioogaravini/hassio-cloudflared:latest-armv7
    container_name: cloudflared-armv7
    restart: unless-stopped
    volumes:
      - ./data:/data
      - ./config:/config
    healthcheck:
      test: ["CMD", "cloudflared --version || exit 1"]
      interval: 5s
      timeout: 10s
      retries: 10