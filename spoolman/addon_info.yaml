source:
  type: docker
  repo: ghcr.io/donkie/spoolman
  current_version: 0.23.1
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-spoolman
build:
  image: gghcr.io/donkie/spoolman
  version_template: "{{major}}.{{minor}}.{{patch}}"
changelog:
  source: github-releases
  repo: Donkie/Spoolman
  version_template: v{{major}}.{{minor}}.{{patch}}
test:
  enable: true
