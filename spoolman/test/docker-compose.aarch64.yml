name: spoolman-aarch64
services:
  spoolman-aarch64:
    image: fabioogaravini/hassio-spoolman:${VERSION:-latest}-aarch64
    build:
      context: ..
      args:
        BUILD_ARCH: aarch64
        BUILD_VERSION: ${VERSION:-latest}
    container_name: spoolman-aarch64
    ports:
      - 7912:8080
    environment:
      - TZ=Etc/UTC
    volumes:
      - data:/data
      - ./options.json:/data/options.json:ro
      - config:/home/app/.local/share/spoolman
      - ssl:/ssl
    healthcheck:
      test: ["CMD", "curl", "-kf", "http://127.0.0.1:8080"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  data:
  config:
  ssl:
