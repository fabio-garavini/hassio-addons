source:
  type: github-releases
  repo: linuxserver/docker-luanti
  current_version: 5.17.0-ls45
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)-ls(?<build>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-luanti
build:
  image: ghcr.io/linuxserver/luanti
  version_template: "{{major}}.{{minor}}.{{patch}}-ls{{build}}"
changelog:
  source: github-releases
  repo: luanti-org/luanti
  version_template: "{{major}}.{{minor}}.{{patch}}"
test:
  enable: true
