name: luanti-aarch64
services:
  luanti-aarch64:
    image: fabioogaravini/hassio-luanti:${VERSION:-latest}-aarch64
    build:
      context: ..
      args:
        BUILD_ARCH: aarch64
        BUILD_VERSION: ${VERSION:-latest}
    container_name: luanti-aarch64
    ports:
      - "30000:30000"
    volumes:
      - data:/data
      - ./options.json:/data/options.json:ro
      - config:/config
      - ssl:/ssl

volumes:
  data:
  config:
  ssl: