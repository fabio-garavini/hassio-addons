name: opencloud-aarch64
services:
  opencloud-aarch64:
    image: fabioogaravini/hassio-opencloud:${VERSION:-latest}-aarch64
    build:
      context: ..
      args:
        BUILD_ARCH: aarch64
        BUILD_VERSION: ${VERSION:-latest}
    container_name: opencloud-aarch64
    ports:
      - 8080:8080
    environment:
      - TZ=Etc/UTC
    volumes:
      - data:/data
      - ./options.json:/data/options.json:ro
      - config:/config
      - share:/share
      - ssl:/ssl
    healthcheck:
      test: ["CMD", "curl", "-kf", "https://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

volumes:
  data:
  config:
  share:
  ssl:
