source:
  type: github-releases
  repo: linuxserver/docker-syncthing
  current_version: v2.0.11-ls201
  version_template: regex:^v(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)-ls(?<build>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 1
  image: fabioogaravini/hassio-syncthing
build:
  image: ghcr.io/linuxserver/syncthing
  version_template: v{{major}}.{{minor}}.{{patch}}-ls{{build}}
changelog:
  source: github-releases
  repo: syncthing/syncthing
  version_template: v{{major}}.{{minor}}.{{patch}}
