#!/command/with-contenv bashio
# shellcheck shell=bash

cd /app || exit 1

# Run migrations first
/usr/local/bin/bun run run-migrations

# Start the application
exec /usr/local/bin/bun ./build/index.js > /dev/null