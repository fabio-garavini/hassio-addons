source:
  type: docker
  repo: goniszewski/grimoire
  current_version: 0.4.4
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)(-hotfix\.(?<build>\d+))?$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 1
  image: fabioogaravini/hassio-grimoire
build:
  image: goniszewski/grimoire
  version_template: "{{major}}.{{minor}}.{{patch}}"
  codenotary: info@fabiogaravini.dev
changelog:
  source: github-releases
  repo: goniszewski/grimoire
  version_template: v{{major}}.{{minor}}.{{patch}}
