source:
  type: docker
  repo: ghcr.io/gibz104/spoolmansync
  current_version: 1.6.3
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-spoolman-sync
build:
  image: ghcr.io/gibz104/spoolmansync
  version_template: "{{major}}.{{minor}}.{{patch}}"
changelog:
  source: none
  version_template: "{{major}}.{{minor}}.{{patch}}"
test:
  enable: true
