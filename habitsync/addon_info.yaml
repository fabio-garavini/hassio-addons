source:
  type: docker
  repo: ghcr.io/jofoerster/habitsync
  current_version: 0.18.1
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 1
  image: fabioogaravini/hassio-habitsync
build:
  image: ghcr.io/jofoerster/habitsync
  version_template: "{{major}}.{{minor}}.{{patch}}"
changelog:
  source: github-releases
  repo: jofoerster/habitsync
  version_template: v{{major}}.{{minor}}.{{patch}}
test:
  enable: false
