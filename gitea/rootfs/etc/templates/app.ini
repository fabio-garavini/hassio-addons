APP_NAME = $APP_NAME
RUN_MODE = prod
WORK_PATH = /data

[repository]
ROOT = /share/gitea/repositories

[repository.local]
LOCAL_COPY_PATH = /share/gitea/tmp/local-repo

[repository.upload]
TEMP_PATH = /share/gitea/uploads

[server]
APP_DATA_PATH = /config
DOMAIN           = $DOMAIN
SSH_DOMAIN       = $SSH_DOMAIN
HTTP_PORT        = 3000
ROOT_URL         = $ROOT_URL
DISABLE_SSH      = false
SSH_PORT         = 222
SSH_LISTEN_PORT  = 22
LFS_START_SERVER = $LFS_START_SERVER

[database]
PATH = /data/gitea.db
DB_TYPE = sqlite3
HOST = localhost:3306
NAME = gitea
USER = root
PASSWD = 
LOG_SQL = false
SCHEMA = 
SSL_MODE = disable

[indexer]
ISSUE_INDEXER_PATH = /config/indexers/issues.bleve

[session]
PROVIDER_CONFIG = /config/sessions

[picture]
AVATAR_UPLOAD_PATH = /config/avatars
REPOSITORY_AVATAR_UPLOAD_PATH = /share/gitea/repo-avatars

[attachment]
PATH = /share/gitea/attachments

[log]
MODE = console
LEVEL = info
ROOT_PATH = /data/logs

[security]
INSTALL_LOCK = $INSTALL_LOCK
SECRET_KEY   = $SECRET_KEY
REVERSE_PROXY_LIMIT = 1
REVERSE_PROXY_TRUSTED_PROXIES = *

[service]
DISABLE_REGISTRATION = $DISABLE_REGISTRATION
REQUIRE_SIGNIN_VIEW  = $REQUIRE_SIGNIN_VIEW

[lfs]
PATH = /share/gitea/lfs