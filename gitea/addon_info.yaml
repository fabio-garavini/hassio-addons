source:
  type: docker
  repo: gitea/gitea
  current_version: 1.25.2
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-gitea
build:
  image: gitea/gitea
  version_template: "{{major}}.{{minor}}.{{patch}}"
changelog:
  source: github-releases
  repo: go-gitea/gitea
  version_template: v{{major}}.{{minor}}.{{patch}}
