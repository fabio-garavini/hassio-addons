---
source:
  type: docker
  image: ghcr.io/blakeblackshear/frigate
  current_version: 0.16.1-rk
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)-(?<flavor>rk)?$
config:
  version_template: '{{major}}.{{minor}}.{{patch}}-{{flavor}}'
  patch: 0
  image: ghcr.io/blakeblackshear/frigate
changelog:
  source: github-releases
  repo: blakeblackshear/frigate
  version_template: v{{major}}.{{minor}}.{{patch}}
