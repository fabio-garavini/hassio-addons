name: calibre-aarch64
services:
  calibre-aarch64:
    image: fabioogaravini/hassio-calibre:${VERSION:-latest}-aarch64
    build:
      context: ..
      args:
        BUILD_ARCH: aarch64
        BUILD_VERSION: ${VERSION:-latest}
    container_name: calibre-aarch64
    ports:
      - 8080:8080
      - 8181:8181
      - 8081:8081
    volumes:
      - data:/data
      - ./options.json:/data/options.json:ro
      - config:/config
      - share:/share
      - media:/media

volumes:
  data:
  config:
  share:
  media: