source:
  type: github-releases
  repo: linuxserver/docker-calibre
  current_version: v8.16.2-ls378
  version_template: regex:^v(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)-ls(?<build>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 0
  image: fabioogaravini/hassio-calibre
build:
  image: lscr.io/linuxserver/calibre:v8.16.2-ls378
  version_template: v{{major}}.{{minor}}.{{patch}}-ls{{build}}
changelog:
  source: none
  version_template: "{{major}}.{{minor}}.{{patch}}"