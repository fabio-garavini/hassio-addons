name: calibre-web-aarch64
services:
  calibre-web-aarch64:
    image: fabioogaravini/hassio-calibre-web:${VERSION:-latest}-aarch64
    build:
      context: ..
      args:
        BUILD_ARCH: aarch64
        BUILD_VERSION: ${VERSION:-latest}
    container_name: calibre-web-aarch64
    ports:
      - "8080:8080"
    volumes:
      - data:/data
      - ./options.json:/data/options.json:ro
      - config:/config
      - ssl:/ssl
      - share:/share
      - media:/media
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8083"]
      interval: 10s
      timeout: 10s
      retries: 3

volumes:
  data:
  config:
  ssl:
  share:
  media: