source:
  type: github-releases
  repo: linuxserver/docker-calibre-web
  current_version: 0.6.25-ls365
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)-ls(?<build>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch}}"
  patch: 1
  image: fabioogaravini/hassio-calibre-web
build:
  image: lscr.io/linuxserver/calibre-web
  version_template: "{{major}}.{{minor}}.{{patch}}-ls{{build}}"
changelog:
  source: github-releases
  repo: janeczku/calibre-web
  version_template: "{{major}}.{{minor}}.{{patch}}"
test:
  enable: true

