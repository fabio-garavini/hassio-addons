# v0.7.0: first Go release

Go rewrite of the FUTO Notes sync server. Wire- and storage-compatible with
the TypeScript server. New installs use SQLite and need no database server.
Existing installs: see docs/UPGRADING_FROM_TYPESCRIPT.md.
