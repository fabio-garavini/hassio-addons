source:
  type: docker
  image: collabora/code
  current_version: 25.04.5.1.1
  version_template: regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch1>\d+)\.(?<patch2>\d+)\.(?<patch3>\d+)$
config:
  version_template: "{{major}}.{{minor}}.{{patch1}}.{{patch2}}.{{patch3}}"
  patch: 0
  image: collabora/code
changelog:
  source: none
  version_template: "{{major}}.{{minor}}.{{patch1}}.{{patch2}}.{{patch3}}"
